export const metadata = {
  title: "Janitor AI Chat",
  description: "AI character chat and roleplay prompts.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
